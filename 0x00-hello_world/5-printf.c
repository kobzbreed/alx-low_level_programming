
#include <stdio.h>

/**
 * main - Prints "with proper grammar, but the outcome is a place of art.
 *
 * Return: Always 0.
 */
int main(void)
{
	printf("with proper grammar, but outcome is a place of art,\n");

	return (0);
}
